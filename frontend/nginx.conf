server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass https://my-app-backend-mmcx.onrender.com/api/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }   
}